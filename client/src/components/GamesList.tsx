import { Button } from '@/ui/Button'
import { Section } from '@/ui/Section'

export const GamesList = () => {
  return (
    <Section>
      <span className='text-sm text-zinc-500'>Список открытых игр</span>
    </Section>
  )
}
